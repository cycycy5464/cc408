# 题目展示改进：全量交互卡片

## 任务清单

- [x] 1. 修改 `question-card.html` partial：从标题链接改为全量交互卡片
- [x] 2. 修改 `chapter-exercises.html`：适配新卡片 + 交互 JS
- [x] 3. 修改 `question/list.html` + `question-list.js`：全量卡片渲染
- [x] 4. 修改 `docs/single.html`：显示相关题目卡片而非试卷链接
- [x] 5. 本地构建验证无报错

## Review

### 改动内容汇总

| 文件 | 改动说明 |
|------|---------|
| `partials/question-card.html` | 标题链接 → `question-block` 完整卡片 + 选项/答案折叠/收藏按钮 + 筛选 data 属性 |
| `question/chapter-exercises.html` | 适配新卡片样式 + 交互 JS（选项/答案/收藏/全部解析）+ 筛选逻辑适配 `.question-block` |
| `question/list.html` | JSON 新增 `content_html` 字段包含全文 HTML；增加交互样式 + `toggleAnswer` 全局函数 |
| `assets/js/question-list.js` | `renderCard()` 改为渲染 `question-block`；新增 `processQuestionBlock()` 处理每个卡片；新增收藏/选项/答案功能；恢复 pagination |
| `docs/single.html` | "相关题目" 从链接到试卷页改为展示最多 5 道相关真题卡片（年份/科目/题型/题名），点击跳转单题页 |

### 构建验证
`hugo --quiet` 无错误 ✅
