---
title: "2025 计算机组成原理 第44题"
date: 2026-07-07
type: question
years:
  - "2025"
subjects:
  - "计算机组成原理"
knowledge_points:
  - "指令流水线"
  - "运算器"
question_type: "comprehensive"
difficulty: 4
source: "408真题"
number: 44
---
接上题，R0~R4 为通用寄存器，SEXT 表示按符号扩展，M中补码除法器，逻辑结构图如下：

> ![原题配图](/cc408/images/questions/2025_Q44_5.svg)

除数寄存器Y

32                      32

ALUop

3 2 位ALU

32

左移

余数寄存器R

写使??

32

6 4 ??

余数/商寄存器Q

控制逻辑 计数??Cn

机器级代码：

// x  ??R2  ??，i   ??R4  ??

//数组 d  的首地址??R3 ??

mov             R1,\(R3\+R4\*4\)//R1←d\[i\]

scov  R1                   //\{RO,R1\}←SEXT\(R1\)

idiv     R1                   //R1<\-\(\{RO,R1\}/R2\)

\(1\)若执行idiv 指令时，d\[i\]=0x87654321, x=0xff, 则补码除法器中R、Q 、Y的初始值分别为多少，\(用十 六进制表示\)?图b 中哪个部分包含计数器?在补码除法器执行过程中，ALUop所控制的ALU运算有哪????

\(2\)假设idiv 执行过程中会检测并触发除法异常，则执行idiv 指令时，哪些情况下会发生除法异常\(要求给出 此时d\[i\]和x 的十六进制机器数\)?发生除法异常时，在异常响应过程中，CPU 需要完成哪些操??

45\. 三个人一起植树，甲挖坑，乙放树苗入坑并填土，丙负责为新种树苗浇水。步骤依次为：挖树坑，放 树苗，填土和浇水。现在有铁锹和水桶各一个，铁锹用于挖树坑，填土。水桶用于浇水。当树坑数量小于 3时，甲才可以挖树坑。设初始??0,铁锹水桶均可用，定义尽可能少的信号量，用wait\(\)  ??signal\(\) ??作描述植树过程中三人的同步互斥关系，并说明所用信号量的作用及其初�??

46\.某进程的虚拟地址空间如图所示，阴影部分为未占用区域，有C 程序??

char   \* ptr;

void   main\(\)\{  int   length;

ptr =\( char \*\) malloc   \(100 \);

scanf \("%s"  ,ptr\);

length     = strlen    \(ptr\);

printf    \("length=%d\\n"    ,length\);

free  \(ptr\);

操作系统内核??

用户??

共享库的存储映射??

动态生成的??

\(运行时由malloc创建\)

??写数据段

只读代码??

未使用区

\(1\)上述程序执行时，PCB 位于哪个区域，执行scanf\( 等待键盘输入时，该进程处于什么状??

\(2\)main    \(函数的代码位于哪个区?? 其直接调用的哪些函数的功能需要通过执行驱动程序实现?

\(3\)变量ptr 被分配在哪个区域?若变??length 没有被分配在寄存器中，则会被分配在哪个区?? ptr  ??向的字符串位于哪个区??

47\. 工程区网络中包含多个子网，并且通过卫星连接到总部服务器，网络架构如下图所示：

轨道高度36000km,   电磁波速度300000 km/s

工程部网??

管理区子??

S1

[10\.10\.10\.33/26](10.10.10.33/26)

卫星

\(作业区子??

R1

生活区子??

Internet

R2

TR1

总部服务??

TR1 ??TR2 为全双工调制解调设备??

卫星链路为R1,R2  之间提供对称全双工信号，每个方向数据

传输率为200kbps

\(1\)忽略卫星信号中继??TR1,TR2     调制解调开销，则R1 ??R2 之间的卫星链路单向传播时延是多少???机H 向总部服务器传输数据时可达到的最大吞吐量是多??若忽略各层协议首部开销，以及以太网的传??时延，则H→sever   上传一??000B 的文件，至少需要多长时??

\(2\)基于 GBN为卫星链路设计单向可靠的链路层协议SLP, 支持R1\->R2' 发送数据。SLP 数据帧长1500B, 忽略ACK帧长度，要求SLP 单向信道利用率不低于80%,则发送窗口至少为?SLP 帧序号至少为多少?

\(3\)总部给工程部分配IP 地址空间10\.10\.10\.0/24,再划分为3个子网，生活区子网不少于120个，作业??网，管理区子网IP 均不少于60个， H 已正确配置IP 。请问管理区子网和生活区子网的地址各是多少?

[tag_link]

正确答案：A
> 本题考察 POP3 协议，对题目提供的选项进行逐一分析??I. 支持用户代理从邮件服务器读取邮件：✅??
> POP3 的主要功能就是支持客户端（用户代理）从邮件服务器读取邮件??
> II. 支持用户代理向邮件服务器发送邮件：�??
> 发送邮件的功能通常??SMTP（Simple Mail Transfer Protocol）负责，而不??POP3??
> III. 支持邮件服务器之间发送与接收邮件：❌??
> 邮件服务器之间的邮件传输通常??SMTP 协议处理，而不??POP3??
> IV. 支持一??TCP 连接收取多封邮件：✅??
> POP3 可以在一个会话中通过一??TCP 连接获取用户邮箱中的多封邮件??
> 所??I、IV 是正确的，本题答案为 A??
> 解答??数据结构 41 设有两个长度均为 n 的一维整型数??A ??res ，对数组 A 中的每个元素 A[i] ，计??A[i] ??A[j](0 ??i ??j ??n-1) 乘积的最大值，并将其保存到 res[i] �??
> 例如，若 A[i] = {1, 4, -9, 6} ，则得到 res[i] = {6, 24, 81, 36} ??
> 现给定数??A ，请设计一个时间和空间上尽可能高效的算??calMulMax ，求 res 中各元素的�??
> 函数原型为： void calMulMax(int A[], int res[], int n) ，要求： (1) 给出算法的基本设计思想：（4 分） (2) 根据设计思想，采??C ??C++ 语言描述算法，关键之处给出注释：?? 分） (3) 说明你所设计算法的时间复杂度和空间复杂度??
> ?? 分） 顺序??查看答案与解??收藏 1）若 A[i] 为负数的话，??A[j] 为子数组 A[i:n] 中的最小值时??A[i] * A[j] 为最大�??
> ??A[i] 为正数的话，??A[j] 为子数组 A[i:n] 中的最大值时??A[i] * A[j] 为最大�??
> 因此算法步骤如下??从右到左遍历数组 A ??维护一个变??maxValue 来存储当前从 i ??n-1 范围内的最大�??
> 维护一个变??minValue 来存储当前从 i ??n-1 范围内的最小�??
> 在每次迭代中，更??maxValue ??minValue ??
> 计算 A[i] * maxValue ??A[i] * minValue ，并将它们的较大值存储在 res[i] �??
> 更新 maxValue ??minValue ：在每次迭代中， maxValue 是当前元素与之前??maxValue 的较大值， minValue 是当前元素与之前??minValue 的较小�??
> 2）算法实现如下： void calMulMax ( int A [], int res [], int n ) { int minValue = INT32_MAX ; int maxValue = INT32_MIN ; for ( int i = n - 1 ; i >= 0 ; i -- ) { if ( A [ i ] < minValue ) { minValue = A [ i ]; } if ( A [ i ] > maxValue ) { maxValue = A [ i ]; } if ( A [ i ] > 0 ) { res [ i ] = A [ i ] * maxValue ; } else { res [ i ] = A [ i ] * minValue ; } } } 3）算法的时间复杂度为 O(n)，空间复杂度??O(1)??
> if(typeof window.quizDB=="undefined"){class e{constructor(){this.dbName="QuizCollectionsDB",this.storeName="quizzes",this.version=1,this.db=null}async init(){return new Promise((e,t)=>{const n=indexedDB.open(this.dbName,this.version);n.onerror=()=>t(n.error),n.onsuccess=()=>{this.db=n.result,e(this.db)},n.onupgradeneeded=e=>{const t=e.target.result;if(!t.objectStoreNames.contains(this.storeName)){const e=t.createObjectStore(this.storeName,{keyPath:"id"});e.createIndex("collectedAt","collectedAt",{unique:!1}),e.createIndex("tags","tags",{unique:!1,multiEntry:!0}),e.createIndex("type","type",{unique:!1})}}})}async add(e){return this.db||await this.init(),new Promise((t,n)=>{const o=this.db.transaction([this.storeName],"readwrite"),i=o.objectStore(this.storeName),s=i.add(e);s.onsuccess=()=>t(s.result),s.onerror=()=>n(s.error)})}async remove(e){return this.db||await this.init(),new Promise((t,n)=>{const o=this.db.transaction([this.storeName],"readwrite"),i=o.objectStore(this.storeName),s=i.delete(e);s.onsuccess=()=>t(s.result),s.onerror=()=>n(s.error)})}async get(e){return this.db||await this.init(),new Promise((t,n)=>{const o=this.db.transaction([this.storeName],"readonly"),i=o.objectStore(this.storeName),s=i.get(e);s.onsuccess=()=>t(s.result),s.onerror=()=>n(s.error)})}async getAll(){return this.db||await this.init(),new Promise((e,t)=>{const s=this.db.transaction([this.storeName],"readonly"),o=s.objectStore(this.storeName),n=o.getAll();n.onsuccess=()=>e(n.result),n.onerror=()=>t(n.error)})}async getAllIds(){return this.db||await this.init(),new Promise((e,t)=>{const s=this.db.transaction([this.storeName],"readonly"),o=s.objectStore(this.storeName),n=o.getAllKeys();n.onsuccess=()=>e(n.result),n.onerror=()=>t(n.error)})}}window.quizDB=new e}function toggleSolutionDetail(e,t){const n=document.getElementById("solution-"+t);n&&(n.style.display==="none"||n.style.display===""?(n.style.display="block",e.textContent="隐藏答案与解??):(n.style.display="none",e.textContent="查看答案与解??))}async function collectAnswerQuiz(e,t){const s=document.getElementById("quiz-"+e);if(!s)return;let n=s.previousElementSibling;const a=[];let o="";for(;n&&n.tagName!=="H5";)a.unshift(n),n=n.previousElementSibling;n&&n.tagName==="H5"&&(o=n.innerText.trim());const i=a.map(e=>{const t=e.cloneNode(!0);return t.querySelectorAll("button, script, style").forEach(e=>e.remove()),t.outerHTML.trim()}).filter(e=>e.length>0),r=document.getElementById("solution-"+e);let c="";if(r){const e=r.cloneNode(!0);e.querySelectorAll("button, script, style").forEach(e=>e.remove()),c=e.innerHTML.trim()}const l=s.dataset.pageUrl,d=getSubjectFromUrl(l),u={id:e,type:"answer",quizNumber:o,question:i.length>0?i:["题目"],questionText:i.map(e=>{const t=document.createElement("div");return t.innerHTML=e,t.textContent.trim()}).join(` `),answer:"",explanation:c,tags:s.dataset.tags,subject:d,pageUrl:s.dataset.pageUrl+`/#${o}`,pageTitle:s.dataset.pageTitle+` ??${o} 题`,collectedAt:(new Date).toISOString()};try{const n=await window.quizDB.get(e);n?(await window.quizDB.remove(e),t.classList.remove("collected"),t.innerHTML=' 收藏',t.title="收藏此题",showNotification("已取消收??)):(await window.quizDB.add(u),t.classList.add("collected"),t.innerHTML=' 已收??,t.title="取消收藏",showNotification("已添加到收藏"))}catch(e){console.error("Error managing answer quiz collection:",e),showNotification("操作失败，请重试")}}if(document.addEventListener("DOMContentLoaded",async function(){try{await window.quizDB.init();const e=await window.quizDB.getAllIds();document.querySelectorAll(".answer-container .collect-btn").forEach(t=>{const n=t.closest(".answer-container");n&&e.includes(n.id.replace("quiz-",""))&&(t.classList.add("collected"),t.innerHTML=' 已收??,t.title="取消收藏")})}catch(e){console.error("Error initializing answer quiz collections:",e)}}),!document.getElementById("quiz-animations")){const e=document.createElement("style");e.id="quiz-animations",e.textContent=` @keyframes slideIn { from { transform: translateX(100%); opacity: 0; } to { transform: translateX(0); opacity: 1; } } @keyframes slideOut { from { transform: translateX(0); opacity: 1; } to { transform: translateX(100%); opacity: 0; } } `,document.head.appendChild(e)} 42 AOE 网，描述 12 个工程活动及持续时间 3 1 6 4 2 7 5 a = 2 b = 5 d = 3 f = 4 e = 3 j = 1 k = 2 c = 1 g = 1 m = 4 h = 1 n = 3 (1) 完成该工程的最短时间是多少？哪些是关键活动??(2) 若以最短时间完成工程，则与活动 e 同时进行的活动可能有哪些??(3) 时间余量最大的活动是哪个？其时间余量是多少??(4) 假设工程从时??0 启动，因某种原因，活??b 在时??6 开始，为保证工程不延期，在其它活动持续时间保持不变的情况下，b 的持续时间最多是多少？若不改??b 的持续时间，则压缩哪个活动的持续时间也能保证工程不延期？ 关键路径 查看答案与解??收藏 1）首先计算最早发生时??ve??node 1 2 3 4 5 6 7 ve 0 9 2 5 12 6 9 然后计算最晚发生时间： node 1 2 3 4 5 6 7 vl 0 10 2 5 12 8 9 ve ??vl 相同的顶点为 1????????，所以关键活动为 a、e、m、n??
> 2）e 的执行时间是 2~5，可以与 e 同时执行的活动为 b、d、c??
> 3）假设有顶点 i→j 的活动（边），则该活动的 时间余量 = vl(j) - ve(i) - 边的长度??
> 计算所有非关键路径上活动的时间余量，可以得到活??j 的时间余量是最大的，为 vl(5) - ve(4) - 1 = 6??
> 4）为保证 1 ??2 ??5 的路径长度不超过关键路径的长度，需要保??6 + b + k ??12，所??b ??4，即 b 的持续时间最长为 4??
> 如果想要保证 b 仍然??5 的话，需要压??k 的时间长度，??k 缩小??1??
> 组成原理 43 计算??M 字长??32 位，按字节编址，数??cache 的数据区大小??32KB，采 8 路组相联，主存块大小??64B，cache 命中时间??2 个时钟周期，缺失损失??200 个时钟周期，采用页式虚拟存储，页大小??4KB??
> 数组 d 的起始地址??0180 0020H（VA31~VA0�??
> (1) 主存地址中的 Cache 组号，块内地址分别占几位？VA 中哪些位可以作为 Cache 索引??
> (2) d[100] ??VA 是多少？d[100] 所在主存块中对应的 Cache 组号是多少？ (3) 设代码已经在 cache 中，i,x 已装入内存，但不??cache，则 d[0] 在其主存块内的偏移量是多少？执行 for 的过程中，访??d ??Cache 缺失率和数组元素的平均访问时间分别是多少？（缺失率用百分比表示，保留两位小数??(4) d 分布在几个页中？若代码已在主存，d 不在主存，则执行 for 的过程中，访??d 所引起的缺页次数是??int x, d[2048], i; for (i = 0; i < 2048; i++) d[i] = d[i]/x; 虚拟页式管理 cache映射方式 缺页异常 查看答案与解??收藏 1）cache 块大小和主存块大小一致为 64B，所以块内偏移为 6 位（ 2 6 = 64 �??
> cache 块的数量??32KB / 64B = 512，由于采??8 路组相连，所以总共??512 / 8 = 64 个组，组号占 6 位（ 2 6 = 64 �??
> 所以第 6 位到??11 位可以作??Cache 索引（用来定位地址可能被哪一组所缓存�??
> 2??d[100] = d + 100 * 4 = 0x0180 0020 + 400 = 0x0180 01B0，对应的二进制为 0000 0001 1000 0000 0000 0001 1011 0000，组号为 000110B ??6??
> 3）物理地址的结构为：页内偏??12 位，物理页号 20 位，所??&d[0] 的页内偏移为 020H = 32??
> 数组总共需要用 2048 * 4 / 64 = 128 个完??cache 块存储，但由于数组第一个元素处于某??cache 的中间（偏移??32），所以总共需??129 ??cache 块存储，在访问每??cache 块中的第一个元素时会发??Cache 缺失??
> 另外需要注意一点，对于指令 d[i] = d[i] / x ，实际上是访问了两次：一次读取，一次写�??
> 所以在计算缺失率和平均访问时间的时候需要考虑这两次访�??
> 缺失??= 129 / (2048 * 2) ??3.15%??
> 数组的平均访问时间为 = (129 * 200 + (4096 - 129) * 2) / 4096 ??8.24 个时钟周�??
> 4）数??d 需要占??8KB 的存储空间，占用两个页面??
> 由于数组的起始地址处于页面内部（偏??32B），所以数??d 分布在三个页面中，触发的缺页次数??3??
> 44 接上题，R0~R4 为通用寄存器，SEXT 表示按符号扩展，M 中补码除法器，逻辑结构图如下： 32 ??ALU 除数寄存??Y 余数寄存??R 被除??商寄存器 Q ALUop 控制逻辑 计数??C n 64 ??32 32 32 32 左移 写使??机器级代码： // x ??R2 中，i ??R4 ??// 数组 d 的首地址??R3 ??mov R1,(R3+R4*4) // R1 ??d[i] scov R1 // {R0,R1} ??SEXT(R1) idiv R1 // R1<-({R0,R1}/R2) (1) 若执??idiv 指令时，d[i]=0x87654321，x=0xff，则补码除法器中 R、Q、Y 的初始值分别为多少（用十六进制表示）？ ??b 中哪个部分包含计数器？在补码除法器执行过程中，ALUop 所控制??ALU 运算有哪几种??(2) 假设 idiv 执行过程中会检测并触发除法异常，则执行 idiv 指令时，哪些情况下会发生除法异常（要求给出此??d[i] ??x 的十六进制机器数�??
> 发生除法异常时，在异常响应过程中，CPU 需要完成哪些操作？ 运算电路 查看答案与解??收藏 【答�??1）R 中的值为 0xffffffff，Q 中的值为 0x87654321，Y 中的值为 0x000000ff??
> b 中的控制逻辑包含计数器，ALUop 所控制??ALU 运算包含加法和减�??
> 2）第一种情况除数为 0 异常，d[i] 为任意值，x ??0x00000000??
> 第二种情况溢出异常，d[i] ??0x80000000，x ??0xffffffff，在 d[i] = -2³¹、x = -1 的情况下会发生溢出异�??
> 在发生除法异常时 CPU 响应的操作： 关中断，修改 CPU 状态为内核�??
> 保存断点（PC ??PSWR 中的值）??
> 跳转到异常处理程�??
> 【解�??1??寄存器初始值分??被除??: d[1] = 0x87654321 ??32 位补码，最高位??1（符号位），??sccv 符号扩展??64 位，??32 位为 0xFFFFFFFF ，低 32 位为 0x87654321 ??
> 除法器结??: 余数寄存??R （高 32 位）和商寄存??Q （低 32 位）组成 64 位被除数，除数寄存器 Y 存储除数??
> 初始??: R : 被除数高 32 ????0xFFFFFFFF Q : 被除数低 32 ????0x87654321 Y : 除数 x = 0xff ??0x000000FF （这里虽然只用了 8 位，但是表示的意思是 255，最高位不是符号位） 计数器位??图中控制逻辑模块包含计数??C ，用于控制除法迭代次数（32 次，对应 32 位前�??
> ALU 运算类型 补码除法采用加减交替法，ALU 需支持两种运算??减法 : R - Y （试减，判断余数符号??加法 : R + Y （若余数为负，恢复余数） 2??除法异常的情??除法异常包括 除以 0 ??商溢????情况 1：除??0 当除??x = 0x00000000 时，无论 d[1] 为何值，触发除以 0 异常??
> 情况 2：商溢出 32 位补码商的范围是 [-2^31, 2^31 - 1] ??
> ??d[1] = 0x80000000 ??-2^31 ??2 位补码最小值）??x = 0xFFFFFFFF ??-1 ）时??商为 (-2^31) / (-1) = 2^31 ，超??32 位补码最�??2^31 - 1 ，触发商溢出异常??
> 异常响应??CPU 操作 异常响应时，CPU 需完成以下核心步骤??保存现场 保存当前程序计数??PC （下一条指令地址）、通用寄存器（或关键字寄存器）状态、程序状态字 PSW （含标志位）??
> 跳转异常处理 从中断向量表加载除法异常对应的服务程序入口地址，更??PC ??
> 模式切换与栈处理 若当前为用户态，切换至内核态，使用内核栈执行异常处理（避免用户栈破坏）??
> 中断控制 可选设置中断屏蔽位，防止嵌套异常干扰处理流程（视架构而定�??
> 操作系统 45 三个人一起植树，甲挖坑，乙放树苗入坑并填土，丙负责为新种树苗浇水??
> 步骤依次为：挖树坑，放树苗，填土和浇�??
> 现在有铁锹和水桶各一个，铁锹用于挖树坑，填土??
> 水桶用于浇水??
> 当树坑数量小??3 时，甲才可以挖树�??
> 设初始坑 = 0，铁锹水桶均可用，定义尽可能少的信号量，??wait() ??signal() 操作描述植树过程中三人的同步互斥关系，并说明所用信号量的作用及其初�??
> 同步问题设计 查看答案与解??收藏 这题是一个近似于流水线的结构，其过程为：挖树坑（甲）??放树苗、填土（乙）??浇水（丙�??
> 不过甲最多可以同时挖三个树苗，也就是说不允许同时存在 4 个未被乙使用的树坑，这是比较复杂的一�??
> 实现甲和乙之间的同步需要使用到 pits ??empty 这两个信号量，同时还需要一??water 信号量来实现乙和丁的同步，代码实现如下： semaphore mutex = 1 ; // 对铁锹的使用需要互??semaphore pits = 3 ; // 甲还能挖洞的数量 sempahore empty = 0 ; // 可以使用的树坑数??sempahore water = 0 ; // 需要浇水的水苗数量 ??() { while ( 1 ) { wait ( pits ); // 最多只能挖三个未被乙使用的??wait ( mutex ); // 占用铁锹 挖树坑；
> signal ( mutex ); // 释放铁锹 signal ( empty ); // 通知乙可以放树苗和填土了 } } ??() { while ( 1 ) { wait ( empty ); // 等待到有树坑为止 wait ( mutex ); // 占用铁锹 放树苗、填土；
> signal ( mutex ); // 释放铁锹 signal ( pits ); // 通知甲可以继续挖坑了 signal ( water ); // 通知丙可以浇水了 } } ??() { while ( 1 ) { wait ( water ); 浇水??
> } }