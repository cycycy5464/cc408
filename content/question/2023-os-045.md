---
title: "2023 操作系统 第45题"
date: 2026-07-07
type: question
years:
  - "2023"
subjects:
  - "操作系统"
knowledge_points:
  - "软件互斥算法"
question_type: "comprehensive"
difficulty: 4
source: "408真题"
number: 45
---

现要求学生使用 swap  指令和布尔型变量 lock 实现临界区互斥。lock  为线程间共享的变量。 lock 的值为 TRUE  时线程不能进入临界区，为 FALSE  时线程能够进入临界区。某同学编写的实现临界  区互斥的伪代码如题45\(a\) 图所示。

newSwap\(\)代码

void newSwap\(bool\*a,bool\*b\) \{

bool temp=\*a;

\*a=\*b;

\*b=temp; \{

某同学编写的伪代码

bool lock=FALSE;  1共享变量

book key=FALSE;

if  \(key==TRUE\)

swap key,lock;//交换key和lock的值

临界区 ；

lock=TRUE;

进入区 ；

退 出 区 ；

题 4 5\(a\) 图                                                          题 4 5\(b\) 图

\(1\)题45\(a\)  图中伪代码中哪些语句存在错误?将其改为正确的语句\(不增加语句条数\)。

\(2\)题45\(b\) 图中给出了两个变量值的函数 newSwap0 的代码是否可以用函数调用语句“newSwap\(&key, &lock\)”代替指令“swap key,lock”以实现临界区的互斥?为什么?
